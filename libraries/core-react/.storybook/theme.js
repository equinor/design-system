import { create } from '@storybook/theming'

const theme = create({
  base: 'light',
  brandTitle: 'Equinor Design System',
  brandImage:
    'https://eds-static.equinor.com/logo/equinor-logo-horizontal.svg#red',
})

export { theme }
