import { tokens } from '@equinor/eds-tokens'

const { colors } = tokens

export const list = {
  color: colors.text.static_icons__default.hex,
  typography: {
    lineHeight: '1.5em',
    fontSize: '1rem',
  },
}
