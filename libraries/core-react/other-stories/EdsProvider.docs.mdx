import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { EdsProvider } from '../..'

# EdsProvider tjobing

export const Template = (args) => <EdsProvider {...args} />

<Canvas>
  <Story name="EdsProvider" id="components-edsprovider--default" args={{ density: 'compact' }} />
  { Template.bind({}) }
</Canvas>

<ArgsTable of={EdsProvider} />
