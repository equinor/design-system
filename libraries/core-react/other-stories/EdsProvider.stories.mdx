import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { EdsProvider } from '../..'

<Meta
  title="Components/EdsProvider"
  argTypes={{
    density: {
      name: 'Density',
      control: 'radio',
      description: 'Available density options',
      options: [
        'comfortable',
        'compact'
      ],
      table: {
        defaultValue: {
          density: 'comfortable'
        },
        type: {
          density: 'where does this end up'
        }
      }
    }
  }}
/>

# EdsProvider

The `<EdsProvider />` is used to provide users with the option of compressing the user interface and see more information at the same time.  
It‘s important **not** to use `compact` as a direct value to `<EdsProvider />` as that makes it harder to use the application for users with disabilities.

<Canvas>
  <Story>
  
  <EdsProvider {...args}>
    <Wrapper>
      <Button>Contained</Button>
      <Button variant="outlined">Outlined</Button>
      <Button variant="ghost">Ghost</Button>
      <Button variant="ghost_icon">
        <Icon data={menu} title="Ghost icon menu"></Icon>
      </Button>
    </Wrapper>
  </EdsProvider>
  </Story>
</Canvas>

<ArgsTable of={ EdsProvider } />
