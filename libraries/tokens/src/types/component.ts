import { Spacing, Borders, Typography, Outline } from './index'

export type ComponentToken = {
  height?: string
  background: string
  spacings: Spacing
  border: Borders
  typography: Typography
  outline?: Outline
}
