name: PR Title Check
on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  check-title:
    runs-on: ubuntu-latest
    steps:
      - name: Check PR Title Format
        run: |
          title="${{ github.event.pull_request.title }}"

          echo "🔍 Checking PR title: '$title'"

          # Check basic format: type: description OR type(scope): description
          if [[ $title =~ ^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\([^)]+\))?!?: .+ ]]; then
            echo "✅ Basic format is correct"
          else
            echo "❌ PR title does not follow conventional commit format"
            echo ""
            echo "Expected format: type(scope): description"
            echo ""
            echo "Valid types: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert"
            echo ""
            echo "Examples:"
            echo "  ✅ feat(eds-core-react): add button component"
            echo "  ✅ fix(eds-icons, eds-core-react): resolve styling issues"
            echo "  ✅ docs: update README"
            echo "  ✅ test: add validation test"
            echo ""
            exit 1
          fi

          # Extract and validate scopes if they exist
          if [[ $title =~ \(([^)]+)\) ]]; then
            scope_part="${BASH_REMATCH[1]}"
            echo "📦 Found scope(s): $scope_part"

            # Split scopes on comma and trim whitespace
            IFS=',' read -ra SCOPE_ARRAY <<< "$scope_part"
            invalid_scopes=()

            for scope in "${SCOPE_ARRAY[@]}"; do
              # Trim whitespace
              scope=$(echo "$scope" | xargs)
              echo "  - Checking scope: '$scope'"

              # Check if scope is valid
              if [[ ! "$scope" =~ ^(design-system-docs|eds-color-palette-generator|eds-core-react|eds-data-grid-react|eds-demo|eds-icons|eds-lab-react|eds-tailwind|eds-tokens|eds-tokens-build|eds-tokens-sync|eds-utils|figma-broker)$ ]]; then
                invalid_scopes+=("$scope")
              fi
            done

            # Report invalid scopes if any
            if [ ${#invalid_scopes[@]} -ne 0 ]; then
              echo "❌ Invalid scope(s) found: ${invalid_scopes[*]}"
              echo ""
              echo "Valid scopes:"
              echo "  - design-system-docs, eds-color-palette-generator, eds-core-react"
              echo "  - eds-data-grid-react, eds-demo, eds-icons, eds-lab-react"
              echo "  - eds-tailwind, eds-tokens, eds-tokens-build, eds-tokens-sync"
              echo "  - eds-utils, figma-broker"
              echo ""
              echo "You can combine multiple scopes like: feat(eds-core-react, eds-utils): description"
              exit 1
            fi

            echo "✅ All scopes are valid"
          else
            echo "📦 No scope specified (optional)"
          fi

          echo "✅ PR title follows conventional commit format!"
