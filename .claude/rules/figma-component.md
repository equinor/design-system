---
paths:
  - 'packages/eds-core-react/src/components/next/**/*.figma.tsx'
---

# Figma-to-Code Component Guidelines (Claude Code)

See [`AGENTS.md`](../../AGENTS.md):

- **Figma-to-Code Workflow** — universal conventions (sub-component prefixes, critical rules, Code Connect example, anti-patterns).
- **Figma MCP workflow** — the `figma_get_design_context` / `figma_get_screenshot` / `figma_get_variable_defs` call sequence and MCP anti-patterns.

This file is path-scoped to `*.figma.tsx` so the canonical guidance surfaces automatically when editing Figma Code Connect files.
