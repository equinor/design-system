/**
 * Shared animation constants.
 * Temporary until animation tokens are added to @equinor/eds-tokens.
 */

/** Standard animation duration in ms (matches Figma). */
export const ANIMATION_DURATION = 150;
