import { Links, PlatformTabs } from './../../../.storybook/components'
import { Primary, Canvas, Controls, Meta } from '@storybook/addon-docs/blocks'
import * as ComponentStories from './EdsProvider.stories'
import MobileDocs from '@equinor/eds-mobile-components/docs/EDSProvider.mdx'

<Meta of={ComponentStories} />

# EdsProvider
Provider used to set global values for EDS react components.

<PlatformTabs mobile={<>
  <Links
    sourceUrl="https://github.com/equinor/design-system-mobile/blob/main/packages/components/src/components/EDSProvider/EDSProvider.tsx"
    npmUrl="https://www.npmjs.com/package/@equinor/eds-mobile-components"
  />
  <MobileDocs />
</>} />

<Links 
    figmaUrl="https://www.figma.com/file/luNobpDIWTuQ9rhyovGLBI/Desktop?node-id=4950%3A0"
    npmUrl="https://www.npmjs.com/package/@equinor/eds-core-react"
    ariaUrl="https://www.w3.org/TR/wai-aria-practices/"
/>

<Primary />
<Controls />



## Usage


Use the `EdsProvider` to encapsulate your whole application.
```typescript
import { EdsProvider } from '@equinor/eds-core-react'

<EdsProvider>
// Your app here
</EdsProvider>
```


## Accessibility

Be aware that only setting `density` to `compact` and not providing option to switch, breaks comformity to WCAG 2.1 guidelines and makes it harder to use the application for users with either permanent disabilities, or temporary disabilities.


## Examples

Examples of how to change density in your application using EDS components.

#### Nested density switcher

Use `useEds` to get & set `EdsProvider` values.  

⚠️ `useEds` only works from nested component inside `EdsProvider` as its built using [React Context](https://reactjs.org/docs/context.html)

<Canvas of={ComponentStories.NestedComponentsDensity} />


#### Custom state

If you have your own state managment you can just set the `density` prop on `EdsProvider`

<Canvas of={ComponentStories.CustomState} />


