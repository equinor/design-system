import { Links } from './../../../../.storybook/components'
import { Primary, Canvas, Controls, Meta } from '@storybook/blocks'
import { UIText } from './UIText'
import * as ComponentStories from './UIText.stories'

<Meta of={ComponentStories} />

# UIText
This component is currently available in the beta release of eds-core-React: <br />
`npm install @equinor/eds-core-react@beta` <br />
It requires the new eds 2.0 design tokens to be included in your app as css variables:
```tsx
//App.tsx or similar
import '@equinor/eds-tokens/css/variables.css'
```
`UIText` uses the `Inter` font. We will likely bundle this on the equinor EDS CDN before the full release, but for now it is available from google fonts:
```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link
  href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap"
  rel="stylesheet"
/>
```
<label className="hide-grid-lines">
  <input className="hide-grid-lines__input" type="checkbox" />
  <UIText className="sb-unstyled">Hide grid lines</UIText>
</label>
<Primary />
<Controls />

## Usage
Text for use within UI elements such as buttons, chips, tabs etc. The text is centered within its textbox by default, but can be set to `onGrid` for cases where this makes sense. Note that for font-sizes where the cap-height is a multiple of 4 there is no difference with `onGrid` on or off regardless.

```tsx
import { UIText } from '@equinor/eds-core-react'
 <UIText size="lg">This is text</UIText>
```
## Sizes

<Canvas of={ComponentStories.Sizes} />

