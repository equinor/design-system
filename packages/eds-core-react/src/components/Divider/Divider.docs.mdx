import { Links } from './../../../.storybook/components'
import { Primary, Canvas, Controls, Meta } from '@storybook/addon-docs/blocks'
import * as ComponentStories from './Divider.stories'

<Meta of={ComponentStories} />

# Divider
Horizontal, thin line.

<Links 
    ariaUrl="https://www.w3.org/TR/2017/REC-wai-aria-1.1-20171214/#separator"
    figmaUrl="https://www.figma.com/file/0bGXR2sCwMVSDNyyzu5BXrO5/UI%E2%80%94User-Interface?node-id=17689%3A101"
    documentationUrl="https://eds.equinor.com/0b0c666ab/p/343afd-divider/b/398446"
    sourceUrl="https://github.com/equinor/design-system/blob/develop/packages/eds-core-react/src/components/Divider/Divider.tsx"
    npmUrl="https://www.npmjs.com/package/@equinor/eds-core-react"
/>

<Primary />
<Controls />

## Usage

The `Divider` brings clarity to layout by grouping and dividing content into groups. <br />
 <ul>
    <li>Dividers should be noticeable in a layout, but not clashing.</li>
    <li>Dividers should only be used if elements can't be split using white space.</li>
    <li>Use dividers occasionally, to create groupings rather than separate individual items.</li>
    <li>Dividers can be used to establish rhythm and hierarchy.</li>
  </ul>

  ```tsx
import { Divider } from '@equinor/eds-core-react'

<Divider />
```

## Examples

### Full-bleed dividers

Full-bleed dividers separate content into segments and span the entire length of a layout.
<ul>
  <li>Separates list and layout elements.</li>
  <li>Indicates where an element may expand.</li>
  <li>Helps separate individual tiles.</li>
</ul>

<Canvas of={ComponentStories.FullBleed} />

### Inset dividers

Use inset dividers to separate related content. 
Use inset dividers when there are anchoring elements such as an icon or avatar.

<Canvas of={ComponentStories.Inset} />

### Middle dividers

Middle dividers in the middle of a layout, separates related content.

<Canvas of={ComponentStories.Middle} />
