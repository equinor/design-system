import styled from 'styled-components'

export const CalendarWrapper = styled.div`
  display: grid;
  grid-gap: 16px;
  max-height: 80vh;
  width: max-content;
  max-width: 560px;
  overflow: auto;
`
