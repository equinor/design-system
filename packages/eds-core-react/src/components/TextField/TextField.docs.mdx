import { Links } from './../../../.storybook/components'
import { Primary, Canvas, Controls, Meta } from '@storybook/addon-docs/blocks'
import * as ComponentStories from './TextField.stories'

<Meta of={ComponentStories} />

# TextField

Lets users enter, interact and edit content, typically in forms and dialogs.

<Links 
    ariaUrl="https://www.w3.org/TR/2017/REC-wai-aria-1.1-20171214/#textbox"
    figmaUrl="https://www.figma.com/file/0bGXR2sCwMVSDNyyzu5BXrO5/UI%E2%80%94User-Interface?node-id=18618%3A6"
    documentationUrl="https://eds.equinor.com/0b0c666ab/p/77e7d5-text-field/b/86826a"
    sourceUrl="https://github.com/equinor/design-system/blob/develop/packages/eds-core-react/src/components/TextField/TextField.tsx"
    npmUrl="https://www.npmjs.com/package/@equinor/eds-core-react"
/>

<Primary />
<Controls />

## Usage

`TextField` should be discoverable, clear and efficient.

<ul>
  <li>Text fields should stand out and point that users can input information.</li>
  <li>Text field states should be **clearly** distinguish from one another.</li>
  <li>Text fields should make it easy to understand the required information and to address any errors.</li>
</ul>

```tsx
import { TextField } from '@equinor/eds-core-react'

<TextField />
```

## Examples

<ul>
  <li>
    **Label text** informs users as to what information is requested for a text
    field. Every text field should have a label.
  </li>
  <li>
    **Helper text** gives additional guidance about the input field, such as how
    it will be used. It should only take up a single line.
  </li>
</ul>

### Types

Examples of common used types (`input` type).

<Canvas of={ComponentStories.Types} />

**Note:** While `type="number"` does provide native validation, the implementation is inconsistent across browsers. We instead recommend using custom validation like the examples provided [here](?path=/docs/inputs-textfield--validation).

**Note:** For multiline text input, please use the [Textarea](../?path=/docs/inputs-textarea--docs) component.

### With units

Units are used for single line text field.

<Canvas of={ComponentStories.WithUnit} />

### With icons

<Canvas of={ComponentStories.WithIcons} />

### Date and time
**Note:** For a more feature rich datepicker please use our [DatePicker](../?path=/docs/inputs-dates-datepicker--docs) and [DateRangePicker](../?path=/docs/inputs-dates-daterangepicker--docs) components.

Textfield can be used with the native `type` prop which allows the usage of `"date"`/`"time"`/`"datetime-local"` to display the browsers built in date/timepickers.
Note that icon should not be used here, as browser implement this input in different ways and some, such as Chrome, add their own icons inside the input.


<Canvas of={ComponentStories.Datepicker} />

### Variants

Examples of validation states. You can add the icon in the helper text or inside the text input, both not in both places.

<Canvas of={ComponentStories.Variants} />

### Read only

<Canvas of={ComponentStories.ReadOnly} />

### Disabled

<Canvas of={ComponentStories.Disabled} />

### Compact

Compact `TextField` uses `EdsProvider`

<Canvas of={ComponentStories.Compact} />

### Validation

How input validation is handled is up to the developer. In this example we use <a href="https://react-hook-form.com/">React Hook Form</a>.

<Canvas of={ComponentStories.ValidationWithReactHookForm} />

Here is one way to do it using `pattern` and `onChange`

<Canvas of={ComponentStories.Validation} />
