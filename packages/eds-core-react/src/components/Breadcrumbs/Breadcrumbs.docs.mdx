import { Links } from './../../../.storybook/components'
import { Meta, Primary, Canvas, Controls } from '@storybook/addon-docs/blocks'
import * as ComponentStories from './Breadcrumbs.stories'

<Meta of={ComponentStories} />

# Breadcrumbs

Show the navigational path to users allowing them to navigate up the hierarchy.

<Links 
    ariaUrl="https://www.w3.org/WAI/ARIA/apg/patterns/breadcrumb/"
    figmaUrl="https://www.figma.com/file/0bGXR2sCwMVSDNyyzu5BXrO5/UI%E2%80%94User-Interface?node-id=17627%3A0"
    documentationUrl="https://eds.equinor.com/0b0c666ab/p/787319-breadcrumbs/b/477a0a"
    sourceUrl="https://github.com/equinor/design-system/blob/develop/packages/eds-core-react/src/components/Breadcrumbs/Breadcrumbs.tsx"
    npmUrl="https://www.npmjs.com/package/@equinor/eds-core-react"
/>

<Primary />
<Controls />

## Usage

`Breadcrumbs` are often placed horizontally before a page's main content.

  ```tsx
import { Breadcrumbs } from '@equinor/eds-core-react'

<Breadcrumbs>
    <Breadcrumbs.Breadcrumb href="#">
      Item1
    </Breadcrumbs.Breadcrumb>
    <Breadcrumbs.Breadcrumb href="#">
      Item2
    </Breadcrumbs.Breadcrumb>
    <Breadcrumbs.Breadcrumb href="#" aria-current="page">
      Item3
    </Breadcrumbs.Breadcrumb>
</Breadcrumbs>
```

## Examples

### Normal

<Canvas of={ComponentStories.Normal} />

### Collapsed

Choose collapse prop to use ellipses to indicate the middle pages. Click ellipses (...) to expand.

<Canvas of={ComponentStories.Collapsed} />

### Truncated Labels

Choose `maxWidth` in pixels to truncate labels. Hover on label to see full text.

<Canvas of={ComponentStories.TruncatedLabels} />

### Wrapped

`Breadcrumbs` by default wraps to the next line if the parent container is too narrow. 
With the `wrap` property set to false, the `Breadcrumbs` will instead automatically truncate. 
It might be a good idea to use `forceTooltip` on the `Breadcrumbs.Breadcrumb` when this is the case.

<Canvas of={ComponentStories.Wrapped} />

### Custom seperator

Seperator can be a character or an `Icon`.

<Canvas of={ComponentStories.CustomSeperator} />

### Third-party routing libraries

Use the `as` prop on `Breadcrumbs.Breadcrumb` to utilize third party routers such as react-router with `Breadcrumbs`

```tsx
import { Link } from "react-router-dom"
import { Breadcrumbs } from '@equinor/eds-core-react'

<Breadcrumbs>
  <Breadcrumbs.Breadcrumb as={Link} to="/">
    Home
  </Breadcrumbs.Breadcrumb>
  <Breadcrumbs.Breadcrumb as={Link} to="/about" aria-current="page">
    About
  </Breadcrumbs.Breadcrumb>
</Breadcrumbs>
```
