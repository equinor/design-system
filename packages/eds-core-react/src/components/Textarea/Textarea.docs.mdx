import { Links } from './../../../.storybook/components'
import { Primary, Controls, Meta } from '@storybook/addon-docs/blocks'
import * as ComponentStories from './Textarea.stories'

<Meta of={ComponentStories} />

# Textarea

⚠️ Helper component - currently only for internal use. Use `Textfield` with `multiline`


<Links 
    ariaUrl="https://www.w3.org/TR/wai-aria-1.1/#textbox"
    sourceUrl="https://github.com/equinor/design-system/blob/develop/packages/eds-core-react/src/components/Textarea/Textarea.tsx"
    npmUrl="https://www.npmjs.com/package/@equinor/eds-core-react"
/>

<Primary />
<Controls />

## Usage

<ul>
    <li>In its simplest form is a multi-line text-field, useful for larger amounts of text.</li>
    <li>**Label text** - every <code>Textarea</code> should have a label.</li>
</ul>
