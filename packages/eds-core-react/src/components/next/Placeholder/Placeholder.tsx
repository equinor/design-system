/**
 * Placeholder component for Code Connect example.
 * This is a minimal component used to demonstrate Figma Code Connect setup.
 */
export const Placeholder = ({ label = 'Placeholder' }: { label?: string }) => {
  return <div>{label}</div>
}
