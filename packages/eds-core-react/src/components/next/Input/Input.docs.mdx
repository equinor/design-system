import { Links } from './../../../../.storybook/components'
import { Primary, Canvas, Controls, Meta } from '@storybook/addon-docs/blocks'
import * as ComponentStories from './Input.stories'

<Meta of={ComponentStories} />

# Input

**⚠️ Beta Component** - This component is under active development and may have breaking changes.

```bash
npm install @equinor/eds-core-react@beta
```

```tsx
import { Input } from '@equinor/eds-core-react/next'
```

Text input and textarea component for forms and user input. Intended to be used if you need more flexibility than the wrapped TextField or select components provide.

<Links
  ariaUrl="https://www.w3.org/TR/wai-aria-1.1/#input"
  sourceUrl="https://github.com/equinor/design-system/blob/develop/packages/eds-core-react/src/components/next/Input/Input.tsx"
  npmUrl="https://www.npmjs.com/package/@equinor/eds-core-react"
/>

<Primary />
<Controls />

## Usage

```tsx
import { Input } from '@equinor/eds-core-react/next'

<Input placeholder="Enter text" aria-label="Description of input" />
```

> **⚠️ Accessibility Note:** When using Input without a visible label (e.g., in search fields or toolbars), you **must** provide an `aria-label` for accessibility. For form fields with visible labels, use the **TextField** component instead, which handles accessibility automatically.

&nbsp;

## Examples

### Types

A selection of native input types.

<Canvas of={ComponentStories.Types} />

### Validation States

<Canvas of={ComponentStories.ValidationStates} />

### Disabled & Read only

<Canvas of={ComponentStories.States} />

### With Label

For a complete form field with label, description, helper message, and required/optional indicators, use the **TextField** component instead. It handles all the composition and accessibility for you.

Input is a low-level building block for custom compositions. When using Input with a visible label, use the `Field` component for proper spacing.

<Canvas of={ComponentStories.WithLabel} />

### Density

<Canvas of={ComponentStories.Compact} />

### Light & Dark Mode

<Canvas of={ComponentStories.ColorSchemes} />

### With Adornments

Use `startText` and `endText` for prefix/suffix text (e.g., "$", "USD", "km"). These stay neutral color in all states.

Use `startAdornment` and `endAdornment` for elements (icons, buttons, etc.) that should inherit state colors (e.g., red when invalid).

<Canvas of={ComponentStories.WithAdornments} />

### Casted as textarea

<Canvas of={ComponentStories.Casted} />

### Override background color

Wrap the Input in a container that sets the `--eds-color-neutral-1` CSS variable to customize the background color. This overrides the base color token that the Input's background references.

<Canvas of={ComponentStories.OverrideBackground} />
