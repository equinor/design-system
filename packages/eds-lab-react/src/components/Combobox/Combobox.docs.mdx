
import { Story, PropsTable } from './../../../.storybook/components'
import * as icons from '@equinor/eds-icons'


# Combobox 
The Combobox component allows users to choose one or multiple options from a list

<Story id="components-combobox--introduction" />
<PropsTable />


## Usage

The value must be chosen from a predefined list of values.

```tsx

const options = [
   { label: 'Microsoft Corporation', symbol: 'MSFT' },
   { label: 'Tesla, Inc', symbol: 'TSLA' },
   { label: 'Apple Inc.', symbol: 'AAPL'},
]

<Combobox 
  label="Stocks"
  options={options}
/>
```

### Option structure

The component accepts the following structure as a minimum. 
```tsx
type ComboboxOption = {
  label: string
}
```
for instance
```tsx
const options = [
   { label: 'Microsoft Corporation', symbol: 'MSFT' },
]
````

## Accessibility
The property `label` is mandatory and make sure you have a descriptive label for screen readers.

## Multiple

Enable the `multiple` property for multiple selection of values
<Story id="components-combobox--multiple" />

## Option label

Change the rendered & searchable value by using the `optionLabel` property.
<Story id="components-combobox--option-label" />

## Read only
<Story id="components-combobox--readonly" />

## Disabled
<Story id="components-combobox--disabled" />


## Disabled option

You can disable an option by setting the `disabled:true` on option
<Story id="components-combobox--disabled-option" />


## Preselected options
<Story id="components-combobox--preselected-options" />


## On options change
<Story id="components-combobox--on-options-change" />

## Compact
Compact `Combobox` using `EdsProvider`
<Story id="components-combobox--compact" />

## With React Hook Form
<Story id="components-combobox--with-react-hook-form" />
