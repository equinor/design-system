*.mdx
README.md
packages/eds-icons/src/data.ts
packages/eds-tokens/build/*
# Generated by the Tokens Studio release pipeline — machine output,
# never hand-formatted (a prettier --write would be reverted by the
# next export run anyway)
packages/eds-tokens/src/tokens/*
packages/eds-core-react/src/components/Select/NativeSelect.tsx
# These packages are written in their own (pre-migration) style —
# double quotes, semicolons — and are linted by their own eslint.config.js,
# not root's. Without this, editor.formatOnSave still rewrites them to
# root's Prettier style (see eslint.config.mjs's matching ignores entry).
packages/eds-mobile-components/**
apps/mobile-storybook/**

# The frozen 1.1.0 docs archive. The format hooks run prettier --write on any
# edited .md/.css, which would reformat the archive as a side effect of an
# unrelated edit; 25 of its files are Prettier-unclean and deliberately left so.
apps/design-system-docs/versioned_docs/**
