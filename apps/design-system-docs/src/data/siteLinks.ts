// Single source of truth for the external URLs referenced across the site.
// This module is plain data (no browser APIs), so it is safe to import from
// Node contexts (docusaurus.config.ts, sidebars.ts) as well as the browser.

export const slackUrl = 'https://equinor.slack.com/channels/eds-design-system'
export const githubUrl = 'https://github.com/equinor/design-system'
export const githubIssuesUrl = `${githubUrl}/issues`
export const storybookUrl = 'https://storybook.eds.equinor.com'
