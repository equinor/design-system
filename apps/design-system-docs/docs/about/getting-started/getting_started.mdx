{/* Intentional redirect: the "Getting started" surface now lives at the /getting-started
    page (src/pages/getting-started.tsx). This stub is kept because doc links still target
    the docs route (e.g. resources.mdx, support.mdx). */}
import {Redirect} from '@docusaurus/router'

<Redirect to="/getting-started" />
