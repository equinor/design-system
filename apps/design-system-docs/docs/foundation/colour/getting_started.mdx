---
title: Getting Started
description: Picking colour tokens. Pairing rules, worked examples, and the mistakes that survive review.
---

import ColourPairing from '@site/src/components/ColourPairing'
import {
  TokenAnatomy,
  ButtonSpecimen,
  DisabledButtonSpecimen,
  BannerSpecimen,
  LinkSpecimen,
  FocusRingSpecimen,
} from '@site/src/components/TokenAnatomy'

Colour in EDS is **semantic**: the tone is part of the token name, and you bind to it directly.
There is one approach, in both design and code.

```css
.button--primary {
  background: var(--eds-background-interactive-accent-emphasis-default);
  color: var(--eds-text-on-emphasis-accent);
}
```

In Figma the same token is `background.interactive.accent.emphasis.default`. Both names are
generated from one definition, so they cannot drift apart.

:::tip Reaching for a hex value? The token exists, so look it up
Every colour token has exactly one Figma name and one CSS custom property, and the
[token reference](./reference.mdx) lists them side by side. If you know the Figma name, the CSS name
is on the same row; if you know the colour by sight, the resolved values in both schemes are there
too.

The two notations line up mechanically, with `--eds-` followed by the dotted name and hyphens for
dots, so you can usually spell one from the other. The reference is still the authority. Each CSS name
is carried in the token's own definition rather than derived from the Figma name, so if the two ever
disagree, the reference is right and the pattern is not.

A hardcoded hex is the failure mode this page exists to prevent. It will not follow the colour
scheme, and nothing will tell you when it has stopped matching the token it was copied from.
:::

:::info Dynamic colour has been removed
Earlier versions offered a second approach, where a `data-color-appearance` attribute decided which
tone a generic token resolved to. It is gone. If you have `data-color-appearance` in your markup or
appearance modes in your Figma file, name the tone explicitly instead.
:::

## Foreground pairing

Every fill below is shown painted, with the text, icon and border tokens it is specified for rendered
inside it. The pairings are worth getting right, because they are what keeps contrast working without
anyone checking it per screen.

<ColourPairing />

Both foreground families are **per tone**. `text.on-muted.danger` is a different value from
`text.on-muted.accent`. Try not to substitute `text.primary` on a tinted fill, because that loses the
tone the tint was there to convey.

:::note Use `text.on-emphasis.<tone>` on a solid fill
`text.on-emphasis.<tone>` is the foreground we recommend on any `emphasis` fill, and it is what the
table above specifies.

`text.inverted` also passes contrast on those fills, so you will not have broken anything by reaching
for it. The reason to prefer `on-emphasis` is that it is defined per tone and follows the fill it sits
on, while `inverted` is one value shared by all six tones.
:::

## Worked examples

### Primary button

<TokenAnatomy
  caption="The rest state. Hover and pressed take the matching values on the same fill."
  annotations={[
    { token: 'text.on-emphasis.accent', edge: 'top', label: 'label' },
    { token: 'background.interactive.accent.emphasis.default', edge: 'bottom', label: 'fill' },
  ]}
>
  <ButtonSpecimen />
</TokenAnatomy>

```css
.button--primary {
  background: var(--eds-background-interactive-accent-emphasis-default);
  color: var(--eds-text-on-emphasis-accent);
}
.button--primary:hover {
  background: var(--eds-background-interactive-accent-emphasis-hover);
}
.button--primary:active {
  background: var(--eds-background-interactive-accent-emphasis-pressed);
}
.button--primary:focus-visible {
  outline: 2px solid var(--eds-border-interactive-focus);
}
```

### Destructive button

The same shape with `danger` in place of `accent`. Every tone follows this pattern, so swapping the
tone is the whole change:

<TokenAnatomy
  caption="The rest state. Hover and pressed take the matching values on the same fill."
  annotations={[
    { token: 'text.on-emphasis.danger', edge: 'top', label: 'label' },
    { token: 'background.interactive.danger.emphasis.default', edge: 'bottom', label: 'fill' },
  ]}
>
  <ButtonSpecimen tone="danger">Delete</ButtonSpecimen>
</TokenAnatomy>

```css
.button--destructive {
  background: var(--eds-background-interactive-danger-emphasis-default);
  color: var(--eds-text-on-emphasis-danger);
}
.button--destructive:hover {
  background: var(--eds-background-interactive-danger-emphasis-hover);
}
.button--destructive:active {
  background: var(--eds-background-interactive-danger-emphasis-pressed);
}
```

Disabled is the one place the pattern breaks:

<TokenAnatomy
  caption="The same button, disabled."
  annotations={[
    { token: 'text.interactive.disabled', edge: 'top', label: 'label' },
    { token: 'background.interactive.disabled', edge: 'bottom', label: 'fill' },
    { token: 'border.interactive.disabled', edge: 'bottom', label: 'stroke' },
  ]}
>
  <DisabledButtonSpecimen />
</TokenAnatomy>

```css
.button--destructive:disabled {
  background: var(--eds-background-interactive-disabled);
  color: var(--eds-text-interactive-disabled);
}
```

**Disabled is neutral, not a faint red.** A control that cannot be actioned has no tone to convey.

### A status banner

A banner is non-interactive. It gets a tint and a border, and **no hover values**:

<TokenAnatomy
  caption="A warning banner. Substitute any tone."
  annotations={[
    { token: 'text.on-muted.warning', edge: 'top', label: 'text' },
    { token: 'background.non-interactive.warning.muted', edge: 'bottom', label: 'fill' },
    { token: 'border.non-interactive.warning.muted', edge: 'bottom', label: 'stroke' },
  ]}
>
  <BannerSpecimen />
</TokenAnatomy>

```css
.banner--warning {
  background: var(--eds-background-non-interactive-warning-muted);
  border: 1px solid var(--eds-border-non-interactive-warning-muted);
  color: var(--eds-text-on-muted-warning);
}
```

If you later make the banner dismissible, the dismiss control becomes interactive. The banner's own
fill does not.

### A link

`link` sits in the tone position but is a role rather than a tone, so it has `default`, `hover` and
`pressed` and no level:

<TokenAnatomy
  caption="A link, as it appears in running text."
  annotations={[
    { token: 'text.interactive.link.default', edge: 'bottom', label: 'link text' },
  ]}
>
  <LinkSpecimen />
</TokenAnatomy>

```css
a {
  color: var(--eds-text-interactive-link-default);
}
a:hover {
  color: var(--eds-text-interactive-link-hover);
  text-decoration-color: var(--eds-border-interactive-link-hover);
}
a:active {
  color: var(--eds-text-interactive-link-pressed);
}
```

`icon.interactive.link.*` matches, for a link that carries an icon. Do not substitute `info` because
links are blue. `info` means the content is informational, and the two are free to diverge.

### Focus ring

One token, every component, on every fill:

<TokenAnatomy
  caption="The focus ring sits outside the element, so it works on any fill."
  annotations={[
    { token: 'border.interactive.focus', edge: 'bottom', label: 'ring' },
  ]}
>
  <FocusRingSpecimen />
</TokenAnatomy>

```css
:focus-visible {
  outline: 2px solid var(--eds-border-interactive-focus);
  outline-offset: 1px;
}
```

## Do's and Don'ts

:::info **Do**

- Pick by **behaviour**. A non-interactive tint and an interactive tint use different tokens even when they
  look identical today
- Use the named `hover` and `pressed` tokens
- Let the scheme attribute do the work
- Use `text.on-muted.<tone>` on a tinted fill
- Treat disabled as its own set

:::

:::danger **Don't**

- Pick by appearance, and give a banner hover values it will be handed by accident later
- Derive states with `opacity` or `filter: brightness()`. They break on tinted fills and invert
  wrongly in dark mode
- Write `[data-color-scheme="dark"] .thing { … }`. If you need that, the token you picked is wrong
- Fall back to `text.primary` on a tint, which discards the tone
- Use a tone's faint end for disabled

:::

## Mistakes that survive review

1. **A raw palette step in a component.** Binding to a numbered scale value instead of a semantic
   token. It looks right and breaks the moment the scale is extended.
2. **Tone on a disabled control.** Disabled has four tokens of its own and no tone.
3. **`text.tertiary` used for body text.** It is for de-emphasised metadata, not a third body colour.
4. **Data-visualisation tokens in UI chrome.** `data-visualization.*` is tuned for telling series
   apart, not for contrast against a surface.

## Coming from an earlier version

If you are moving an existing app or Figma file across, the state and strength names have changed and
some tokens have no direct equivalent. [Migration](./migration.mdx) covers both routes, from EDS 1.x
and from 2.0.0-beta.

## Switching colour scheme

By default the system reads the `data-color-scheme` attribute:

```html
<!-- Light (default) -->
<html data-color-scheme="light"></html>
<!-- Dark -->
<html data-color-scheme="dark"></html>
```

Set it once, at the top of the page or app, so the whole interface switches together. That is what
people expect from a colour scheme, and it is what we recommend.

The attribute can be set on any ancestor and it cascades, so scoping a single panel to dark inside a
light page is technically possible. We would not advise it. A colour scheme describes the interface,
not a region inside it.

## For designers

Add the EDS colour library to your Figma file and pick tokens by name. The variable names are
identical to the ones above, so a design and its implementation refer to the same thing.

Only the semantic layer is published to the variable picker. The numbered scale underneath it is
hidden on purpose, so there is no way to bind a raw palette value to a component by accident. See
[The palette](./palette.mdx).

### Tokens are scoped to the property they belong on

The variable picker is filtered by what you are colouring. Open the fill picker and you get fill
tokens; open the stroke picker and you get border tokens. Text and icon tokens do not appear where a
background belongs, and vice versa.

This is not a setting to switch on, because the scoping is part of each token's definition. Two
consequences are worth knowing:

- **The list you choose from is already short.** Most colour tokens are irrelevant to any given
  property, and you are never shown them.
- **A whole class of mistake is simply unavailable.** You cannot bind `text.primary` to a rectangle's
  fill, or drop a background token onto a stroke.

So if a token you expected is missing from a picker, that absence is usually the answer: it belongs
on a different property.
