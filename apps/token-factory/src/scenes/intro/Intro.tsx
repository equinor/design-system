// Intro scene. The centred narrator carries the whole stage — this
// component renders nothing. It exists so the scene registry can resolve
// 'intro' uniformly alongside every other scene id.

export function Intro() {
  return null
}
