---
title: Progress indicators
metaTitle: Progress indicators usage
tabs:
  - Overview
  - Usage
toc:
  - Figma
  - API
mode: publish
route: /components/progress-indicators/usage/
---

import {
  CircularProgress,
  DotProgress,
  LinearProgress,
  StarProgress,
} from '@equinor/eds-core-react/dist/core-react.cjs.js'

## Figma

### How to add

1. Locate the **Assets** tab in the **Layers Panel**.
2. There are two ways to locate the `Progress indicator` components needed:
   - Use the search bar to search for a component grouping name or variation name.
   - Scroll through the folders list and open the relevant grouping.
3. Once the component needed is located, click and drag it into the frame/artboard.

## API

### CircularProgress

<PropsTable props={CircularProgress} />

### DotProgress

<PropsTable props={DotProgress} />

### LinearProgress

<PropsTable props={LinearProgress} />

### StarProgress

<PropsTable props={StarProgress} />
