---
title: Popover
metaTitle: Popover overview
tabs:
  - Overview
  - Usage
mode: publish
route: /components/popovers/
---

import { Popover, Icon, Typography, Button } from '@equinor/eds-core-react/dist/core-react.cjs.js'

A _popover_ is a floating card that provides more information or actions on hover or click.

<!--One to two sentences as an introduction to the component-->

## Guidelines

<!--List how to use it, sizes, widths, grid interactions, color allowances -->

A popover provides excess or additional information that did not fit in the main content area. A popover always has an indicator (caret) towards the area it is attached to. Popovers should never be wider than 560px and their height should not be taller than 80% of the screen.

To dismiss a popover, use the close icon, press the `ESC` key, open another popover or click outside the popover. If there are no actions in the popover, then the close icon should be the first focusable element.

**Don't**

<!--Bullet point list if needed -->

- Do not use for a single short line of text, use a _Tooltip_ instead.
- Do not have multiple popovers open at the same time.

<FigmaImage
  url="https://www.figma.com/file/0bGXR2sCwMVSDNyyzu5BXrO5/UI%E2%80%94User-Interface?node-id=2717%3A0"
  alt="Figma design of popover"
/>

## Demo

<Playground>
  {() => {
    const [active, setActive] = React.useState(null)
    const handleClick = (event) => {
      setActive(event.currentTarget.id)
    }
    const handleClose = () => {
      setActive(null)
    }
    const dialogMarkup = (
      <>
        <Popover.PopoverTitle>Title </Popover.PopoverTitle>
        <Popover.PopoverContent>
          <Typography variant="body_short">Content</Typography>
        </Popover.PopoverContent>
        <div style={{ display: 'flex' }}>
          <Button onClick={handleClose} style={{ marginRight: '1rem' }}>
            Cancel
          </Button>
          <Button onClick={handleClose}>OK</Button>
        </div>
      </>
    )
    return (
      <div
        style={{
          display: 'grid',
          gridGap: '32px',
          gridTemplateColumns: 'repeat(3, fit-content(100%))',
        }}
      >
        <Popover
          onClose={handleClose}
          open={active === '1'}
          placement="topLeft"
        >
          <Popover.PopoverAnchor>
            <Button id="1" onClick={handleClick}>
              Top left
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover onClose={handleClose} open={active === '2'} placement="top">
          <Popover.PopoverAnchor>
            <Button id="2" onClick={handleClick}>
              Top
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover
          onClose={handleClose}
          open={active === '3'}
          placement="topRight"
        >
          <Popover.PopoverAnchor>
            <Button id="3" onClick={handleClick}>
              Top right
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover
          onClose={handleClose}
          open={active === '4'}
          placement="bottomLeft"
        >
          <Popover.PopoverAnchor>
            <Button id="4" onClick={handleClick}>
              Bottom left
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover onClose={handleClose} open={active === '5'} placement="bottom">
          <Popover.PopoverAnchor>
            <Button id="5" onClick={handleClick}>
              Bottom
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover
          onClose={handleClose}
          open={active === '6'}
          placement="bottomRight"
        >
          <Popover.PopoverAnchor>
            <Button id="6" onClick={handleClick}>
              Bottom right
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover
          onClose={handleClose}
          open={active === '7'}
          placement="leftTop"
        >
          <Popover.PopoverAnchor>
            <Button id="7" onClick={handleClick}>
              Left top
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover onClose={handleClose} open={active === '8'} placement="left">
          <Popover.PopoverAnchor>
            <Button id="8" onClick={handleClick}>
              Left
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover
          onClose={handleClose}
          open={active === '9'}
          placement="leftBottom"
        >
          <Popover.PopoverAnchor>
            <Button id="9" onClick={handleClick}>
              Left bottom
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover
          onClose={handleClose}
          open={active === '10'}
          placement="rightTop"
        >
          <Popover.PopoverAnchor>
            <Button id="10" onClick={handleClick}>
              Right top
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover onClose={handleClose} open={active === '11'} placement="right">
          <Popover.PopoverAnchor>
            <Button id="11" onClick={handleClick}>
              Right
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
        <Popover
          onClose={handleClose}
          open={active === '12'}
          placement="rightBottom"
        >
          <Popover.PopoverAnchor>
            <Button id="12" onClick={handleClick}>
              Right bottom
            </Button>
          </Popover.PopoverAnchor>
          {dialogMarkup}
        </Popover>
      </div>
    )
  }}
</Playground>

### Activiation types

Popovers can be activated by hover or click. To dismiss a popover, use
the close icon, press the ESC key, open another popover or click
outside the popover. If there are no actions in the popover, then the
close icon should be the first focusable element.

<Playground>
  {() => {
    const [active, setActive] = React.useState(null)
    const handleClick = (event) => {
      setActive(event.currentTarget.id)
    }
    const handleClose = () => {
      setActive(null)
    }
    return (
      <Popover onClose={handleClose} open={active === '1'} placement="top">
        <Popover.PopoverAnchor>
          <Button id="1" onClick={handleClick}>
            On Click
          </Button>
        </Popover.PopoverAnchor>
        <Popover.PopoverTitle>Title </Popover.PopoverTitle>
        <Popover.PopoverContent>
          <Typography variant="body_short">Content</Typography>
        </Popover.PopoverContent>
        <div style={{ display: 'flex' }}>
          <Button onClick={handleClose} style={{ marginRight: '1rem' }}>
            Cancel
          </Button>
          <Button onClick={handleClose}>OK</Button>
        </div>
      </Popover>
    )
  }}
</Playground>

Remember to use both onMouseEnter and onFocus attributes to your trigger
element to be able to open the popover by tab iterations. A timeout
delay on onMouseEnter is recommended to avoid unwanted trigger of the
popup while browsing.

<Playground>
  {() => {
    const [active, setActive] = React.useState(null)
    const handleClick = (event) => {
      setActive(event.currentTarget.id)
    }
    const handleHover = (event) => {
      const current = event.currentTarget.id
      setActive(current)
    }
    const handleClose = () => {
      setActive(null)
    }
    return (
      <Popover onClose={handleClose} open={active === '1'} placement="top">
        <Popover.PopoverAnchor>
          <Button id="1" onMouseEnter={handleHover} onFocus={handleClick}>
            On hover
          </Button>
        </Popover.PopoverAnchor>
        <Popover.PopoverTitle>Title </Popover.PopoverTitle>
        <Popover.PopoverContent>
          <Typography variant="body_short">Content</Typography>
        </Popover.PopoverContent>
        <div style={{ display: 'flex' }}>
          <Button onClick={handleClose} style={{ marginRight: '1rem' }}>
            Cancel
          </Button>
          <Button onClick={handleClose}>OK</Button>
        </div>
      </Popover>
    )
  }}
</Playground>
