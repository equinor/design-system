---
title: Slider
tabs:
  - Overview
  - Usage
toc:
  - Guidelines
  - Variations
  - Demo
mode: publish
route: /components/slider/
---

import { Slider } from '@equinor/eds-core-react'

A _slider_ is a flexible input that allows users to adjust predefined values.

<!--One to two sentences as an introduction to the component-->

## Guidelines

<!--List how to use it, sizes, widths, grid interactions, color allowances -->

Sliders reflect values along a bar where users can select a single value or a range of values. Sliders reflect the current state of the values they control, and value changes are immediate to the content in which they belong.

### Labels

Number and abbreviation labels are allowed. They should clearly communicate the values such as: Min., Max, Avg. or a number.

**Don't**

<!--Bullet point list if needed -->

- Do not use a slider without a title indicating what the slider controls.
- Do not use for ranges that are extremely different, e.g., 1-10000.
- Do not use for ranges that are very small, e.g., 1-4.

## Variations

<!--One ### heading per frame in Figma, explaining each component in that frame. One 'content' section per variation if needed -->

### Dot

On hover, a tooltip can appear to give more guidance.

<FigmaImage
  url="https://www.figma.com/file/0bGXR2sCwMVSDNyyzu5BXrO5/Desktop-UI?node-id=2544%3A419"
  alt="Figma design of slider dot"
/>

### Handle

On hover, a tooltip can appear to give more guidance.

<FigmaImage
  url="https://www.figma.com/file/0bGXR2sCwMVSDNyyzu5BXrO5/Desktop-UI?node-id=2544%3A418"
  alt="Figma design of slider handle"
/>

### Indicator

The indicator signifies the selected range.

<FigmaImage
  url="https://www.figma.com/file/0bGXR2sCwMVSDNyyzu5BXrO5/Desktop-UI?node-id=2544%3A417"
  alt="Figma design of slider indicator"
/>

### Track

<FigmaImage
  url="https://www.figma.com/file/0bGXR2sCwMVSDNyyzu5BXrO5/Desktop-UI?node-id=2544%3A416"
  alt="Figma design of slider track"
/>

## Demo

### Slider simple

<Playground>
  <>
    <div>
      <span id="simple-slider">Simple slider</span>
      <Slider value={4} min={0} max={10} ariaLabelledby="simple-slider" />
    </div>
    <div style={{ marginTop: '2rem' }}>
      <span id="even-simpler-slider">
        Simple slider, no dots, no min or max values, steps of 10
      </span>
      <Slider
        ariaLabelledby="even-simpler-slider"
        value={50}
        min={0}
        max={100}
        step={10}
        minMaxDots={false}
        minMaxValues={false}
      />
    </div>
    <div style={{ marginTop: '2rem' }}>
      <label id="disabled-slider">Disabled slider</label>
      <Slider value={50} disabled={true} ariaLabelledby="disabled-slider" />
    </div>
  </>
</Playground>

## Slider with range

<Playground>
  {() => {
    const [value, updateValue] = React.useState([30, 70])
    const [valueTwo, updateValueTwo] = React.useState([0, 500])
    const [valueTwoCommited, updateValueTwoCommited] = React.useState([0, 500])
    const onChange = (event, value) => {
      updateValue(value)
    }
    return (
      <>
        <div>
          <span id="range-slider-label">Range slider</span>
          <Slider
            value={value}
            onChange={onChange}
            ariaLabelledby="range-slider-label"
          />
          <p style={{ marginTop: '1.5rem' }}>
            <small>Output from slider is {value.join(', ')}</small>
          </p>
        </div>
        <div style={{ marginTop: '2rem' }}>
          <span id="range-slider-label-with-mouseevent">
            Range slider with a lot of steps
          </span>
          <Slider
            value={valueTwo}
            onChange={(event, value) => {
              updateValueTwo(value)
            }}
            min={0}
            max={500}
            ariaLabelledby="range-slider-label-with-mouseevent"
            onChangeCommitted={(event, value) => {
              updateValueTwoCommited(value)
            }}
          />
          <p style={{ marginTop: '1.5rem' }}>
            <small>
              Committed output from slider is {valueTwoCommited.join(', ')}
            </small>
          </p>
        </div>
        <div style={{ marginTop: '2rem' }}>
          <span id="large-step-range-slider">Range slider with steps of 5</span>
          <Slider
            ariaLabelledby="large-step-range-slider"
            step={5}
            min={30}
            minMaxDots={false}
            value={[40, 60]}
          />
        </div>
      </>
    )
  }}
</Playground>
