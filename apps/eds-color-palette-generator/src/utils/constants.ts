/**
 * Default color constants used throughout the color palette generator
 */

/** Fallback gray color used when color data is missing or invalid */
export const FALLBACK_GRAY_COLOR = '#808080'

/** Default color for new color anchors */
export const DEFAULT_ANCHOR_COLOR = '#888888'
